# Copy to .env.local and fill in before running locally.
# Must also be set in the Vercel dashboard before deploying.

# Full URL of the deployed app — used by PWA manifest and any absolute URL generation
# Example: https://your-app.vercel.app
NEXT_PUBLIC_APP_URL=
